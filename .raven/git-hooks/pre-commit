#!/usr/bin/env sh

repo_root=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
cd "$repo_root" || exit 0

just check

# Optional secret scan for staged changes.
if command -v gitleaks >/dev/null 2>&1; then
    gitleaks git --staged --redact
fi
