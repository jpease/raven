"""Behavioral evaluation scenarios for Raven's shipped guidance.

Run by `scripts/eval.py` on demand against a local agent CLI; never part of a
quality gate. See `docs/evaluation.md` for the method and its limits.
"""
